@model IEnumerable<AcademicosUem.Models.DocenteAssociado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Docente.apelido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Funcao.Designacao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trabalho.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Data)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Docente.apelido)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Funcao.Designacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trabalho.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Data)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
